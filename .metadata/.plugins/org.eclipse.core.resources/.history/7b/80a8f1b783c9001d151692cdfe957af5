package tcp_client;

import java.net.Socket;

public class ClientsManager {
	public static void main(String[] args) {
		Socket s = new Socket("localhost", 7777);

		BasicClient c = new BasicClient(s);
	}
}

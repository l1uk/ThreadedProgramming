package tcp_server;

import java.net.ServerSocket;

public class ServerManager {
	public void main(String[] args) {
		ServerSocket listener = new ServerSocket(7777);
		new Thread(new BasicRequestHandler(listener));
	}
}
